export default interface ICoverData {
                                        page?: string;
                                        title?: string;
                                        content?: string;
                                        showButton?: boolean;
                                        linkButton?: string;
                                        pathImage: string;
                                    }
